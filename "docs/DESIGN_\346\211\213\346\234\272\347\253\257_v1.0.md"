# 每日任务清单 · 手机端产品设计（v1.0）

> 状态：**草案，待确认**（用户要求：先出方向方案，暂不动手配置环境）
> 关联文档：`docs/DESIGN_v1.1.md`（电脑端 v1.1 产品设计）、`docs/技术方案_v1.1.md`
> 目标平台：**Android（安卓）**。iOS 不在本期范围（苹果 Xcode 只能在 Mac 上运行，Windows 无法打包）

---

## 1. 定位：电脑端的「手机版兄弟」，不是替代品

在手机上做一个**独立的、长得一样的**每日任务清单，用来**随时随地记一笔、随手勾掉**。

- 界面与交互**和电脑端保持一致**（米色 UI、每日视图、统计、就地编辑），不用重新学。
- **数据各存各的、互不同步** —— 沿用 v1.1 已确定的「不联网、不做多端同步」决策。
  - 手机上加的任务不会出现在电脑上，反之亦然。
  - 想搬数据，用**已有的「导出 / 导入 JSON」**手动搬（这是两端唯一的桥梁）。
- 依然**纯本地、不联网**，延续这个项目的核心原则。

## 2. 整体方向（一句话）

> **一套前端代码 + 两种存储后端**：电脑浏览器里继续走 Python 后端；手机 App 里走 JS 本地存储。

- 前端（米色 UI、每日视图、统计图、双击编辑）**一行不改，100% 复用**。
- 唯一的新代码：**把 `app.py` 里那些纯函数（增删改、统计、完成时间、导入导出）用 JS 重写一份**，约 250 行。
- 运行环境判断只用一行：Capacitor 提供的 `isNativePlatform()`。
- **电脑端完全不受影响**，Python 零依赖后端原样保留。

这样做的额外好处：手机端不再需要跑任何服务器，因此**没有 localhost、没有端口、没有跨域、没有"安卓禁止 http 明文"这些坑**，App 秒开，也不会被安卓的后台策略杀掉。

## 3. 主要功能

### 3.1 每日视图（与电脑端一致）
- 任务带 `date` 归属日期，打开默认「今天」
- 顶部日期导航：前一天 / 今天 / 后一天，可翻看任意历史日期
- **只能给「今天」加任务**；历史日期可勾选完成、可编辑内容、可删除，但不能新增
- 过期未完成任务**留在原日期、不顺延**

### 3.2 统计与打卡回顾（与电脑端一致）
- 今日完成 / 总数、本周完成、连续打卡天数、今日完成率
- 近 7 天完成数趋势图（纯 SVG，零依赖，今天高亮）

### 3.3 任务操作
- 新增、勾选完成 / 取消、删除
- **就地编辑**：改任务文字，保存后归属日期不变
- **必须保留中文输入法的兼容处理**（电脑端刚踩过的坑：回车选词不能被当成保存）

### 3.4 导出 / 导入 JSON
- 导出：把清单存成 JSON 分享出去（或存到手机）
- 导入：选一个 JSON 文件恢复
- 这是手机 ↔ 电脑搬数据的**唯一途径**，必须保留

### 3.5 简化版自动备份
- 每次改动后在手机本地**保留最近几次快照**（建议 5 次），误删可还原
- 比电脑端的 `backup/`（保留 30 份）轻量，符合手机使用场景

### 3.6 移动端适配（手机特有，电脑端没有的）
- **触控友好**：勾选框、删除按钮的点击区域放大到手指尺寸
- **安全区适配**：适配刘海屏、挖孔屏和底部手势条，内容不被遮挡
- **返回键**：安卓返回键 / 侧滑返回要有合理行为（例如统计面板打开时先关面板）
- **竖屏锁定**：清单类应用竖屏更合适
- **软键盘**：手机输入法的「完成 / 换行」键行为要专门处理（见第 6 节风险）

## 4. 明确不做
- ❌ 多端同步 / 联网 / 云存储（沿用 v1.1 决策）
- ❌ 提醒与通知（v1.1 就不做）
- ❌ 标签 / 分类 / 优先级、深色模式、拖拽排序
- ❌ iOS 版本（Windows 打包不了）
- ❌ 上架应用商店（自己用，数据线侧载即可）

## 5. 技术方案要点（简述，细节留到技术方案文档）

| 项 | 选择 |
|---|---|
| 打包方式 | **Capacitor**（把网页装进安卓壳） |
| 手机端存储 | **浏览器 localStorage**（简单可靠，不会出权限问题） |
| 自动备份 | 本地保留最近 5 次快照 |
| 图标 | 复用现有 `assets/` 图标，自动生成安卓各尺寸 |
| 安装包 | **debug APK**（自用，无需签名）；将来要 release 再生成 keystore |
| 安装方式 | 数据线拷进手机点安装，或 `adb install` |

### 5.1 环境安装一律放 D 盘（用户明确要求）

C 盘只留必要内容，安卓开发相关的大件全部装 D 盘：

| 内容 | 安装路径 |
|---|---|
| Android Studio | `D:\Android\Android Studio` |
| Android SDK | `D:\Android\Sdk` |
| Gradle 缓存 | `D:\Android\.gradle`（环境变量 `GRADLE_USER_HOME`） |
| npm 缓存 | `D:\Android\.npm`（`npm config set cache`） |
| 安卓虚拟设备（如需） | `D:\Android\.android` |

需要设置的环境变量：`ANDROID_HOME`、`ANDROID_SDK_ROOT` 指向 `D:\Android\Sdk`。

**已确认的现有环境**：Node.js v22.22.2 + npm 10.9.7 ✅ 已有；JDK 22 ✅ 已有；安卓 SDK / adb ❌ 需安装；D 盘剩余 109 GB ✅ 充足（预计占用 20~30 GB）。

## 6. 实施步骤（粗排）

1. **装环境**：Android Studio + SDK → 全部装 D 盘，并验证 `adb` 可用
2. **接入 Capacitor**：初始化安卓工程、生成图标
3. **写 JS 存储层**：移植 `app.py` 的纯逻辑（增删改 / 统计 / 完成时间 / 导入导出 / 快照备份）
4. **前端改造**：加环境判断（JS 存储 ↔ HTTP 接口）+ 移动端适配（触控 / 安全区 / 返回键 / 软键盘）
5. **构建 APK → 数据线安装到手机**
6. **测试 + 提交**（遵循 AGENTS.md：改完要 commit + 测试全绿）

## 7. 风险与待确认

| # | 事项 | 说明 |
|---|---|---|
| 1 | **手机软键盘的回车键** | 手机输入法的「完成 / 换行」键不一定触发标准的 Enter keydown，编辑保存可能失效。需要专门处理（如监听 form 提交或软键盘确认事件）。**这是手机端最可能踩的坑。** |
| 2 | **双击在手机上会触发缩放** | 电脑端是「双击文字编辑」，但手机上双击通常用于放大页面。建议**手机端改为「点一下文字即编辑」**（手机没有 hover，单击也更自然），电脑端保留双击。需你确认。 |
| 3 | **卸载 / 清理会清空数据** | localStorage 存在 App 内部，卸载 App 或系统清理会丢数据。靠「导出 JSON」兜底，需要在文档里提醒定期导出。 |
| 4 | Android Studio 首次配置较繁琐 | 只需配置一次，之后构建都是一条命令。 |

### 需要你拍板的细节
1. 手机端编辑触发方式：**单击文字编辑（推荐）** 还是沿用双击？
2. 是否**锁定竖屏**？
3. App 名称（默认「每日任务清单」）与包名是否就用默认值？
