# 每日任务清单 · 手机端产品设计（v1.0）

> 状态：**方案已完善**（2026-08-31）。已确认项：**单击文字编辑 / 锁定竖屏 / 名称「To Do」**
> 关联文档：`docs/DESIGN_v1.1.md`（电脑端 v1.1 产品设计）、`docs/技术方案_v1.1.md`
> 目标平台：**Android（安卓）**。iOS 不在范围（苹果 Xcode 只能在 Mac 上运行，Windows 无法打包）
> 当前进度：**尚未开始配置环境**，本方案确认后再动手

---

## 1. 定位：电脑端的「手机版兄弟」，不是替代品

在手机上做一个**独立的、长得一样的**每日任务清单，用来**随时随地记一笔、随手勾掉**。

- 界面与交互和电脑端保持一致（米色 UI、每日视图、统计、就地编辑），不用重新学。
- **数据各存各的、互不同步** —— 沿用 v1.1 已定的「不联网、不做多端同步」决策。
- 依然**纯本地、不联网**。

### 1.1 「数据独立」带来的后果（必须提前知道）

| 现象 | 说明 |
|---|---|
| 手机上加的任务，电脑上没有 | 反之亦然。两端是两份互不干扰的清单 |
| 统计数字两端各自独立 | 手机上的「连续打卡 N 天」和电脑上的**不会合并**，是两个独立的记录 |
| 搬数据只能手动 | 通过「导出 JSON → 传到手机 → 导入」完成（见 4.4） |
| 卸载 App 会清空数据 | localStorage 存在 App 内部。**务必定期导出备份** |

## 2. 整体方向（一句话）

> **一套前端代码 + 两种存储后端**：电脑浏览器里走 Python 后端；手机 App 里走 JS 本地存储。

- 前端（米色 UI、每日视图、统计图、就地编辑）**一行不改，100% 复用**。
- 唯一的新代码：**把 `app.py` 里那些纯函数用 JS 重写一份**（约 250 行）。
- 环境判断一行代码：Capacitor 的 `isNativePlatform()`。
- **电脑端完全不受影响**，Python 零依赖后端原样保留。

额外好处：手机端不再需要跑服务器，因此**没有 localhost、端口、跨域、安卓禁止 http 明文**这些坑，App 秒开，也不会被安卓后台策略杀掉。

## 3. 主要功能

### 3.1 每日视图（与电脑端一致）
- 任务带 `date` 归属日期，打开默认「今天」
- 顶部日期导航：前一天 / 今天 / 后一天
- **只能给「今天」加任务**；历史日期可勾选、可编辑、可删除，但不能新增
- 过期未完成任务**留在原日期、不顺延**

### 3.2 统计与打卡回顾（与电脑端一致）
- 今日完成 / 总数、本周完成、连续打卡天数、今日完成率
- 近 7 天完成数趋势图（纯 SVG，今天高亮）

### 3.3 任务操作
- 新增、勾选完成 / 取消、删除
- **就地编辑**：改文字后保存，**归属日期不变**
- **必须保留中文输入法兼容**（电脑端刚踩过的坑：回车选词不能被当成保存）

### 3.4 导出 / 导入 JSON（两端唯一的桥梁）

**搬运流程（电脑 → 手机）：**
1. 电脑上点「导出备份」得到 `tasks.json`
2. 用微信 / QQ / 数据线把它传到手机
3. 手机 App 里点「导入备份」选中该文件

反向（手机 → 电脑）流程相同。

**规则：**
- **格式必须兼容电脑端**：手机端导入要能直接吃电脑导出的 `tasks.json`（同字段：`id/content/done/created_at/date/completed_at`），并做**同样的字段归一化**（缺 `date` 用 `created_at` 日期补齐等），保证来回搬不丢数据。
- **导入语义 = 覆盖**（与电脑端一致，简单可预期）；覆盖前自动存一份快照，误导可还原。
- 不做「合并」——合并会带来去重、冲突一大堆麻烦，自用场景覆盖 + 快照已足够。

**实现方式（已确认）：**

| 方向 | 做法 |
|---|---|
| **导出** | 先把 JSON 写成临时文件（**Filesystem** 官方插件）→ 再调起系统分享面板（**Share** 官方插件），可发微信 / 存到「文件」。<br>*为什么是两个？* Share 插件只能分享「文件路径」或「纯文本」；要分享一个真正的 `.json` 文件，得先用 Filesystem 把它落盘拿到的路径。两者都是 Capacitor 官方插件，体积小、无广告。 |
| **导入** | 沿用原生 `<input type="file">`（和电脑端一致，零插件）。**需真机验证**；若 WebView 不支持再换社区文件选择插件。 |

### 3.5 简化版自动备份 + 撤销

- **每次改动「前」**把当前完整数据存为一份快照，手机本地**保留最近 5 份**
  （与电脑端「改动后备份」略有不同：改前快照**直接服务于「误删还原」**——删完立刻能退回删除前的状态）
- 提供**「撤销上一次改动」**入口：删错了、改错了，一键退回
- 快照存在 localStorage，与电脑端的 `backup/` 目录（保留 30 份）互相独立

### 3.6 删除的防误触（手机特有）

手机容易误点，但弹确认框又啰嗦。方案：**删除后弹出短暂提示条「已删除 · 撤销」**（约 4 秒），点「撤销」即恢复；不点则自动消失。

### 3.7 移动端适配（手机特有，电脑端没有）

| 项 | 处理 |
|---|---|
| **触控区域** | 勾选框、删除按钮、文字三块区域**清晰分开**且放大到手指尺寸（≥44px），避免误触 |
| **编辑触发** | **单击文字进入编辑**（已确认）。手机无鼠标悬停，且双击会触发页面缩放 |
| **取消编辑** | 电脑上按 Esc 取消，**手机没有 Esc 键** → 编辑时在输入框旁显示「保存 / 取消」两个小按钮；安卓返回键 = 取消编辑 |
| **跨天刷新** | App 在后台过夜后「今天」会变。监听「App 回到前台 / 页面重新可见」，发现日期已变就自动刷新到新的今天（否则过了零点还停在昨天、且无法新增） |
| **安全区** | 适配刘海屏、挖孔屏、底部手势条，内容不被遮挡 |
| **软键盘** | 键盘弹出时确保输入框可见不被遮挡；键盘的「完成」键要能触发保存（见第 8 节风险） |
| **屏幕方向** | **锁定竖屏**（已确认） |
| **返回键** | 统计面板开着时先关面板；编辑中按返回 = 取消编辑 |
| **窄屏排版** | 日期导航条、统计指标卡、趋势图在窄屏下重新适配（图表内字号适当放大，避免缩放后看不清） |

## 4. 明确不做
- ❌ 多端同步 / 联网 / 云存储（沿用 v1.1 决策）
- ❌ 提醒与通知（v1.1 就不做）
- ❌ 标签 / 分类 / 优先级、深色模式、拖拽排序
- ❌ iOS 版本、上架应用商店
- ❌ 导入时的「合并」逻辑（只做覆盖）

## 5. 技术方案要点

| 项 | 选择 |
|---|---|
| 打包方式 | **Capacitor**（把网页装进安卓壳） |
| 手机端存储 | **浏览器 localStorage**（简单可靠、无权限问题） |
| 存储键名 | 数据 `dailytodo.tasks.v1`；快照 `dailytodo.snapshots.v1` |
| 自动备份 | 改动前快照，保留最近 5 份 + 「撤销上一次改动」 |
| 图标 | 复用现有 `assets/` 图标自动生成安卓各尺寸（注意安卓自适应图标的安全区，见风险） |
| 应用名 / 包名 | 名称 **To Do**、包名 **`com.zcs.todo`**（均已确认） |
| 用到的官方插件 | **Filesystem** + **Share**（仅为了导出时能分享真正的 `.json` 文件） |
| 安装包 | **debug APK**（自用无需签名）。**务必始终用同一种方式打包** |
| 安装方式 | 数据线拷进手机点安装，或 `adb install` |

### 5.1 环境安装一律放 D 盘（用户明确要求）

| 内容 | 安装路径 |
|---|---|
| Android Studio | `D:\Android\Android Studio` |
| Android SDK | `D:\Android\Sdk` |
| Gradle 缓存 | `D:\Android\.gradle`（环境变量 `GRADLE_USER_HOME`） |
| npm 缓存 | `D:\Android\.npm`（`npm config set cache`） |
| 安卓虚拟设备 | `D:\Android\.android` |

环境变量：`ANDROID_HOME`、`ANDROID_SDK_ROOT` → `D:\Android\Sdk`

**现有环境**：Node.js v22.22.2 + npm 10.9.7 ✅；JDK 22 ✅；安卓 SDK / adb ❌ 待装；C 盘剩 206G、D 盘剩 109G（安卓相关预计占 20~30G）。

### 5.2 文件组织与仓库卫生

- 新增 **`static/store.js`**：手机端的存储 + 统计 + 备份逻辑，写成标准 JS 模块，可被 Node 直接 import 做单测。
  （现有前端是单文件 `index.html`；拆出这一个文件是为了可测试、职责清晰，是本次唯一的结构调整）
- Capacitor 会生成 `android/`、`package.json`、`node_modules/`、`capacitor.config.json`。
- **必须更新 `.gitignore`**：`node_modules/`、`android/app/build/`、`android/.gradle/`、`android/local.properties`、`*.keystore` 等（避免把构建产物和签名密钥提交进仓库）。

## 6. 测试方案（遵循 AGENTS.md：改完要有测试且全绿）

| 范围 | 方式 |
|---|---|
| **JS 存储与统计逻辑** | 用 **Node 22 自带的测试运行器 `node --test`** —— **零第三方依赖**，可直接 import `static/store.js` 做单测。覆盖：增删改、勾选写/清 `completed_at`、导入字段归一化、快照与撤销、统计（今日/本周/连续打卡/完成率/近 7 天趋势） |
| **电脑端不回归** | 现有 44 个 Python 测试必须继续全绿；前端改动后 `tests/test_app.py` 里的前端标记用例（双击触发 / 输入法保护 / 空内容提示）需同步适配 |
| **真机验收** | 装到手机上跑一遍：加 → 勾 → 改 → 删 → 撤销 → 统计 → 导出 → 导入 → 翻历史日期 |

## 7. 实施步骤

1. **装环境**：Android Studio + SDK → 全部 D 盘，验证 `adb` 可用
2. **接入 Capacitor**：初始化安卓工程、生成图标、更新 `.gitignore`
3. **写 `static/store.js`**：移植 `app.py` 纯逻辑（增删改 / 统计 / 完成时间 / 导入导出 / 快照与撤销）
4. **前端改造**：环境判断（JS 存储 ↔ HTTP 接口）+ 手机端适配（单击编辑 / 保存取消按钮 / 跨天刷新 / 安全区 / 窄屏排版 / 返回键）
5. **写 `node --test` 单测**并跑通
6. **构建 debug APK → 数据线安装到手机**，真机验收
7. **提交**（AGENTS.md 要求）

## 8. 风险与已识别的坑

| # | 风险 | 应对 |
|---|---|---|
| 1 | **手机软键盘的「完成」键不一定触发标准 Enter keydown** → 编辑保存失效 | 除 keydown 外，额外监听表单 submit / 输入框的确认事件；并在编辑态提供明确的「保存」按钮兜底 |
| 2 | **手机上双击会触发页面缩放** | 已改为单击编辑（已确认）；同时在 WebView 层禁用双击缩放 |
| 3 | **手机没有 Esc 键，无法取消编辑** | 编辑态加「保存 / 取消」按钮；返回键 = 取消 |
| 4 | **App 后台跨天**导致停在昨天 | 监听回到前台 / 页面可见性，日期变化时刷新 |
| 5 | **卸载或清理会清空 localStorage** | 定期导出 JSON；文档与界面都做提醒 |
| 6 | **安卓自适应图标安全区**：图标内容需落在中心 66% 区域内，现有图标可能贴边被裁 | 用 `@capacitor/assets` 生成后真机检查，必要时重做带安全边距的前景图 |
| 7 | **localStorage 超出配额**会抛异常 | 捕获 `QuotaExceededError` 并提示导出备份、清理旧数据 |
| 8 | **换签名会导致无法覆盖安装**（需先卸载 → 数据丢失） | 始终用同一种打包方式（debug APK），不要中途换 keystore |
| 9 | Android Studio 首次配置较繁琐 | 只需一次，之后构建一条命令 |
| 10 | **导入的文件选择**依赖 WebView 里的 `<input type="file">` | 真机优先验证；不行就换成社区文件选择插件（方案已备好） |
| 11 | 忘了「两端要手动搬数据」这回事 | 首次启动时给一句轻提示：可从电脑导出 `tasks.json` 后导入；设置页也放一句导出提醒 |

## 9. 状态：全部确认完毕，可以开工

| 项 | 结论 |
|---|---|
| 目标平台 | Android（数据线侧载安装） |
| 数据 | 各用各的、不同步；靠导出 / 导入 JSON 手动搬 |
| 技术路线 | Capacitor（一套前端 + 两种存储后端） |
| 手机端存储 | localStorage + 改动前快照（5 份）+ 撤销 |
| 编辑触发 | 单击文字进入编辑（配「保存 / 取消」按钮） |
| 导出 / 导入 | Filesystem + Share 分享文件；原生 file input 导入 |
| 屏幕 | 锁定竖屏 |
| 名称 / 包名 | To Do / `com.zcs.todo` |
| 环境安装位置 | 全部 D 盘（`D:\Android\`） |

**下一步（等你下令才开始）**：装 Android Studio + SDK 到 D 盘 → 验证 adb。
