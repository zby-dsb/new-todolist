# 每日任务清单 · 技术方案（v0.1 MVP）

> 关联文档：`docs/DESIGN.md`（产品设计 / 需求）
> 技术栈状态：已锁定 —— 本地网页应用 · Python 标准库后端（零依赖）· HTML/CSS/JS 前端 · 本地 `tasks.json` · 导出/导入双备份
> 已确认决策：导入=先自动备份再覆盖 · 后台完全隐藏运行 · 顶部显示今天日期

## 1. 目标与范围
按 `DESIGN.md` 的 v0.1 实现最小可用闭环：网页界面完成 **add / list / done / rm**，数据自动存本地 `tasks.json`，并提供**导出 / 导入**做备份。不实现日期分任务、标签、提醒、同步。

## 2. 总体架构
```
┌──────────────┐   HTTP (fetch)   ┌────────────────────┐   读写    ┌──────────────┐
│  浏览器前端   │ ───────────────▶ │  Python 后端        │ ───────▶ │ tasks.json   │
│ static/index  │ ◀─────────────── │  (http.server, 零依赖)│ ◀─────── │ (本地文件)   │
└──────────────┘   返回 JSON/HTML  └────────────────────┘           └──────────────┘
        │                     │
        │ 导入前自动备份       │ 启动写 PID / 日志
        ▼                     ▼
  tasks.backup.*.json      server.pid / app.log
```
- 前端只跑在浏览器里，所有数据操作都通过后端接口完成。
- 后端仅监听 `127.0.0.1`（本机），不对外暴露。
- 后台进程**完全隐藏运行**（无可见窗口），通过 `start.bat` 启动、`stop.bat` 停止。

## 3. 目录结构（计划）
```
new-todolist/
├── app.py                 # 后端：路由 + 接口 + 静态页面服务 + 自启浏览器 + PID/日志
├── static/
│   └── index.html         # 前端：米色 UI（复用 demo/archive/index2.html 并接入接口）
├── tasks.json             # 数据文件（运行时生成，gitignore）
├── server.pid             # 运行中的进程号（gitignore，供 stop.bat 停止用）
├── app.log                # 运行日志（gitignore，出问题时排查）
├── tasks.backup.*.json    # 导入前的自动备份（gitignore）
├── tests/
│   └── test_app.py        # 单测 + 冒烟测试
├── start.bat              # 双击启动（隐藏运行 + 自动开浏览器）
├── stop.bat               # 双击停止
├── .gitignore             # 忽略 tasks.json / server.pid / app.log / 备份 / __pycache__
└── docs/
    ├── DESIGN.md          # 产品设计
    └── 技术方案.md         # 本文
```
> 说明：`demo/archive/index.html`、`demo/archive/index2.html` 是前期可视化原型，仅作界面参考；正式前端以 `static/index.html` 为准，原型文件保留不删。

## 4. 后端技术方案
### 4.1 技术选型
- Python 3 标准库：`http.server`（服务 + 路由，用 `ThreadingHTTPServer` 提升体验）、`json`、`webbrowser`（自启浏览器）、`os`/`logging`（PID 与日志）。
- **不引入任何第三方库**，保证「只装 Python 就能跑」。
- **隐藏运行**：用 `pythonw.exe`（而非 `python.exe`）启动，不弹控制台窗口。

### 4.2 接口设计（同源 REST 风格）
| 方法 | 路径 | 说明 | 请求体 / 参数 | 返回 |
|------|------|------|---------------|------|
| GET  | `/`            | 返回前端页面 `static/index.html` | — | HTML |
| GET  | `/static/<file>`| 返回静态资源（限定在 static 目录内，防越权） | — | 文件内容 |
| GET  | `/api/tasks`   | 列出全部任务 | — | `{tasks:[...], today:"2026-08-30"}` |
| POST | `/api/tasks`   | 新增任务 | `{content:"..."}` | 新建的任务对象 |
| POST | `/api/tasks/{id}/toggle` | 切换完成状态 | 路径 id | 更新后的任务对象 |
| DELETE | `/api/tasks/{id}` | 删除任务 | 路径 id | `{ok:true}` |
| GET  | `/api/export`  | 导出为 json 文件下载 | — | `tasks.json` 附件 |
| POST | `/api/import`  | **先自动备份当前数据，再用请求体 JSON 覆盖导入** | 原始 JSON 文本 | `{ok:true, count:n}` |

> 说明：`{id}` 为任务数字编号；非法 id / 空内容返回 4xx + 错误信息。导入采用「请求体直接传 JSON 文本」而非文件上传，避免标准库解析上传格式的麻烦。

### 4.3 存储层设计
- `tasks.json` 结构：`[{"id":1,"content":"...","done":false,"created_at":"2026-08-30T15:00:00"}, ...]`
  - 新增 `created_at` 字段：现在不使用，但为后续「每日/历史」版本铺路，避免将来大改数据结构。
- **编码强制 UTF-8**：所有文件读写显式指定 `encoding='utf-8'`，杜绝 Windows 下中文乱码。
- 启动时若文件不存在则初始化为空列表；每次写操作后**原子写**（先写临时文件再 `replace`）避免半截文件。
- **导入前自动备份**：执行覆盖前，先把当前 `tasks.json` 复制为 `tasks.backup.<时间戳>.json`，再写入导入内容。
- 新任务 `id` = 当前最大 id + 1（空列表时从 1 开始）；删除后 id 不回收，列表按 id 顺序展示（允许空缺）。
- 将「读写 JSON / 增删改 / 备份」抽成纯函数（如 `load_tasks(path)`、`add_task(tasks, content)`、`backup_tasks(path)`），**不依赖 socket**，方便单测。

### 4.4 关键实现点
- 路由：用 `BaseHTTPRequestHandler`，在 `do_GET/do_POST/do_DELETE` 中按 `path` 分发；「业务逻辑」与「HTTP 处理」分离，使逻辑可测。
- 静态资源**防越权**：解析 `/static/...` 时做路径归一化，禁止 `..` 跳出 static 目录。
- 请求体读取：`Content-Length` + `rfile.read`；JSON 用 `json.loads`（导入直接读原始文本再 `json.loads`）。
- 响应：统一 `application/json`（接口）或 `text/html`（页面），均设 `Content-Type` 与 UTF-8。
- **端口自适应**：默认 `8000`，若被占用则自动顺延到下一个空闲端口（如 8001…），启动后把实际端口记到 `app.log` 并用于自开浏览器。
- **隐藏运行与启停**：
  - 启动：`app.py` 写入自身 PID 到 `server.pid`，调用 `webbrowser.open` 打开 `http://127.0.0.1:<实际端口>/`，然后 `serve_forever()`；日志写入 `app.log`。
  - 停止：`stop.bat` 读取 `server.pid` 执行 `taskkill /PID <pid> /F` 并删除该文件。
  - `start.bat` 增加「已在运行则不再启动」的防护（检测 `server.pid` 存在且端口可用）。

## 5. 前端技术方案
### 5.1 界面
- 直接复用 `demo/archive/index2.html` 的米色 UI（标题居中、输入区在下、暖色调），把「内存假数据」改为「调接口拿真数据」。
- **顶部显示今天日期**：标题「每日任务清单」下方用一行显示当天日期（如「2026年8月30日 星期日」），由前端用浏览器本地时间渲染（`toLocaleDateString('zh-CN', ...)`），强化「每日」感（暂不按日期分任务）。

### 5.2 与后端交互
- `add`：读取输入框 → `POST /api/tasks` → 重新 `GET /api/tasks` 渲染。
- `list`：进入页面 `GET /api/tasks` 渲染列表 + 统计「共 N · 已完成 M」。
- `done`：点方框 → `POST /api/tasks/{id}/toggle` → 重渲染。
- `rm`：点删除 → `DELETE /api/tasks/{id}` → 重渲染。
- 渲染时对 `content` 做 HTML 转义，防止 XSS。
- **空清单友好提示**：无任务时显示「还没有任务，在下面添加一个吧～」。
- **错误提示**：接口失败时前端弹出一个小提示（而非静默失败），便于发现问题。

### 5.3 导出 / 导入
- 导出：`<a href="/api/export" download="tasks.json">` 触发下载。
- 导入：`<input type="file">` 选 json → 前端用 `FileReader` 读成文本 → `POST /api/import` 把文本作为请求体发出 → 成功后刷新列表。后端会先自动备份再覆盖。

## 6. 数据模型（tasks.json 示例）
```json
[
  {"id": 1, "content": "复习 STM32 定时器", "done": false, "created_at": "2026-08-30T15:00:00"},
  {"id": 2, "content": "写操作系统实验报告", "done": true,  "created_at": "2026-08-30T16:20:00"},
  {"id": 3, "content": "健身 30 分钟",       "done": false, "created_at": "2026-08-30T18:00:00"}
]
```

## 7. 测试方案（遵循 AGENTS.md）
- **存储层单测**（不启服务）：临时 `tasks.json`，覆盖 `add` / `toggle` / `delete` / 空列表初始化 / `backup` 生成 / `export→import` 往返一致。
- **接口单测**：把路由逻辑包成可调用函数，用 `unittest.mock` 模拟请求/响应；或起 `ThreadingHTTPServer` + `urllib` 实跑关键路径（测试时用**系统分配的空闲端口**，避免与 8000 冲突）。
- **冒烟测试**：脚本启动 `app.py`，依次 `add → list → toggle → delete → export → import`，断言状态码与数据正确，最后关服务。
- 交付前 `python -m unittest` 全绿方可提交。

## 8. 运行与部署（本机 · Windows）
- 日常使用：双击 `start.bat` → 后台**隐藏**启动小程序 → 浏览器自动弹出清单。
- 关闭：双击 `stop.bat`（或在任务管理器结束 `pythonw` 进程）；数据已写入 `tasks.json`，不丢。
- 开发调试：`python app.py`（可见窗口 + 日志），浏览器开 `http://127.0.0.1:<端口>`。
- 隐藏模式下日志落在 `app.log`，若打不开清单先看该文件。

## 9. 安全与边界
- 仅监听 `127.0.0.1`，不对外网开放；单用户本地使用，**不做登录鉴权**。
- 静态资源限定在 `static/` 内，禁止路径穿越（`../`）读取本机其他文件。
- 输入校验：任务内容非空、长度上限（如 500 字）；前端转义防 XSS。
- `import` 仅接受合法任务数组（校验字段类型），忽略多余字段，防止脏数据；且**导入前必做自动备份**，避免误操作丢数据。

## 10. 风险与待确认
- `http.server` 为轻量服务，单用户足够；已用 `ThreadingHTTPServer` 改善并发体验。
- 隐藏运行（`pythonw`）下出错不弹窗，依赖 `app.log` 排查；已在方案中固化日志记录。
- 数据文件 `tasks.json`、`server.pid`、`app.log`、备份文件均加入 `.gitignore`，避免把个人清单/运行态提交进仓库。
- 跨天「每日」语义、标签、提醒等按计划留到 v1.1 / v2+，不在本次实现；`created_at` 字段已预埋以便平滑升级。
