# 每日任务清单 · 技术方案（v1.1）

> 关联文档：`docs/DESIGN_v1.1.md`（产品设计 / 需求）
> 技术栈状态：已锁定 —— 本地网页应用 · Python 标准库后端（零依赖）· 原生 HTML/CSS/JS 前端 · 本地 `tasks.json`（项目目录内）+ `backup/` 自动备份 · 双击桌面图标运行 · 不联网
> 承接版本：v0.1 MVP（add/list/done/rm + 导出导入 + `tasks.json` 持久化）
> 已确认决策：每日视图（仅今天可加、历史仅查看、过期不顺延）· 统计与打卡回顾 · 编辑任务内容 · 数据落盘项目目录 + 每次改动自动备份

## 1. 目标与范围
在 v0.1 最小可用闭环之上，按 `DESIGN_v1.1.md` 落地轻量增强，不引入任何第三方依赖：

- **每日视图**：任务带 `date`，首页默认今天，可前后翻看历史；新增只能加到今天；过期未完成任务留在原日期、不顺延。
- **统计与打卡回顾**：今日完成/总数、本周完成、连续打卡天数、今日完成率、近 7 天 SVG 趋势图（统计面板）。
- **编辑任务内容**：点文字就地改，保存即更新（日期不变）。
- 延续：导出/导入（导入前自动备份再覆盖）、原子写、UTF-8、防路径穿越、XSS 转义、隐藏运行、桌面图标。

不实现：标签/分类/优先级、提醒/通知、深色模式、拖拽排序、多端同步（见设计文档第 4 节）。

## 2. 总体架构
```
┌──────────────┐   HTTP (fetch)   ┌────────────────────┐   读写    ┌──────────────┐
│  浏览器前端   │ ───────────────▶ │  Python 后端        │ ───────▶ │ tasks.json   │
│ static/index  │ ◀─────────────── │  (http.server, 零依赖)│ ◀─────── │ (项目目录内) │
└──────────────┘   返回 JSON/HTML  └────────────────────┘           └──────────────┘
        │                     │                          │
        │ 导入前自动备份       │ 每次改动自动备份          │ 启动写 PID / 日志
        ▼                     ▼                          ▼
  tasks.backup.*.json      backup/<日期>.json        server.pid / app.log
```
- 前端只跑在浏览器内，所有数据操作通过后端接口完成。
- 后端仅监听 `127.0.0.1`（本机），不对外暴露。
- 后台进程**完全隐藏运行**（无可见窗口），通过 `start.vbs` 启动、`stop.bat` 停止。
- **自动备份**：任何写操作（增/删/改/导入覆盖）成功后，立即把最新 `tasks.json` 复制一份到 `backup/` 目录（按时间戳命名），与 v0.1「仅导入前备份」相比覆盖更全。

## 3. 目录结构（更新版）
```
new-todolist/
├── app.py                 # 后端：路由 + 接口 + 静态页面服务 + 自启浏览器 + PID/日志
├── static/
│   └── index.html         # 前端：米色 UI（v1.1 每日视图 + 统计面板 + 就地编辑）
├── tasks.json             # 数据文件（运行时生成，gitignore）
├── server.pid             # 运行中的进程号（gitignore）
├── app.log                # 运行日志（gitignore）
├── backup/                # v1.1 自动备份目录（gitignore）
│   └── tasks.<时间戳>.json
├── tasks.backup.*.json    # 导入前的额外备份（gitignore，与 backup/ 并存）
├── tests/
│   └── test_app.py        # 单测 + 冒烟测试（含 v1.1 新接口）
├── start.bat / start.vbs  # 双击启动（隐藏运行 + 自动开浏览器）
├── stop.bat               # 双击停止
├── install_desktop_icon.vbs # 一次性生成桌面「每日任务清单.lnk」快捷方式
├── assets/icon.ico        # 桌面图标
├── .gitignore             # 忽略 tasks.json / server.pid / app.log / backup/ / 备份 / __pycache__
└── docs/
    ├── DESIGN.md          # v0.1 产品设计
    ├── DESIGN_v1.1.md     # v1.1 产品设计
    ├── 技术方案.md         # v0.1 技术方案
    └── 技术方案_v1.1.md    # 本文
```
> 说明：`demo/archive/index.html`、`demo/archive/index2.html`、`demo/archive/index_v1.1.html` 为前期可视化原型（含纯前端模拟 demo），仅作界面参考；正式前端以 `static/index.html` 为准。

## 4. 后端技术方案
### 4.1 技术选型
- Python 3 标准库：`http.server`（`ThreadingHTTPServer` 提升并发体验）、`json`、`webbrowser`、`os`/`logging`、`datetime`、`collections`。
- **不引入任何第三方库**，保证「只装 Python 就能跑」。
- **隐藏运行**：用 `pythonw.exe` 启动，不弹控制台窗口。
- 日期计算全部用标准库 `datetime.date`（本地时区按系统时区），避免引入 `pytz` 等库。

### 4.2 接口设计（同源 REST 风格）
| 方法 | 路径 | 说明 | 请求体 / 参数 | 返回 |
|------|------|------|---------------|------|
| GET  | `/`            | 返回前端页面 `static/index.html` | — | HTML |
| GET  | `/static/<file>`| 返回静态资源（限定 static 目录内，防越权） | — | 文件内容 |
| GET  | `/api/tasks`   | 列出任务，**支持按日期过滤** | `?date=YYYY-MM-DD`（缺省=今天） | `{tasks:[...], today:"2026-08-30", viewed:"2026-08-30"}` |
| POST | `/api/tasks`   | 新增任务（**归属今天，忽略传入 date**） | `{content:"..."}` | 新建任务对象 |
| POST | `/api/tasks/{id}/toggle` | 切换完成状态；完成时写 `completed_at`，取消则清空 | 路径 id | 更新后的任务对象 |
| PUT  | `/api/tasks/{id}` | **编辑任务内容**（仅 content，日期不变） | `{content:"..."}` | 更新后的任务对象 |
| DELETE | `/api/tasks/{id}` | 删除任务 | 路径 id | `{ok:true}` |
| GET  | `/api/stats`   | **统计与打卡回顾** | — | 见 4.3 节 stats 结构 |
| GET  | `/api/export`  | 导出为 json 文件下载 | — | `tasks.json` 附件 |
| POST | `/api/import`  | **先自动备份当前数据，再用请求体 JSON 覆盖导入** | 原始 JSON 文本 | `{ok:true, count:n}` |

> 说明：`{id}` 为任务数字编号；非法 id / 空内容返回 4xx + 错误信息。导入采用「请求体直接传 JSON 文本」而非文件上传，避免标准库解析上传格式的麻烦。新增任务强制 `date=今天`，即便前端误传 date 也以服务端今天为准，落实「只能加到今天」。

### 4.3 存储层设计
- `tasks.json` 单任务结构：
  ```json
  {"id": 1, "content": "复习 STM32 定时器", "done": false,
   "created_at": "2026-08-30T15:00:00",
   "date": "2026-08-30", "completed_at": null}
  ```
  - `date`：归属日期（`YYYY-MM-DD`），新增默认今天；统计与每日视图据此分组。
  - `completed_at`：标记完成时写 ISO 时间，取消完成时清空为 `null`；用于算「完成率 / 本周完成 / 趋势 / 连续打卡」。
- **编码强制 UTF-8**：所有读写显式 `encoding='utf-8'`，杜绝中文乱码。
- **原子写**：每次写操作先写临时文件再 `os.replace`，避免半截文件损坏。
- **自动备份（v1.1 强化）**：每次成功写操作后，把当前 `tasks.json` 复制为 `backup/tasks.<YYYYMMDD-HHMMSS>.json`；保留最近 N 份（如 30 份）防无限膨胀，旧备份可手动回滚。与 v0.1「导入前备份」并存。
- **旧数据归一化（兼容升级）**：`load_tasks` 对每条任务补齐字段——缺 `date` 用 `created_at` 的日期；`done=True` 且缺 `completed_at` 时近似取 `created_at`；缺 `created_at` 用现在时间。保证 v0.1 老数据升级到 v1.1 **不丢、不报错**。
- 新任务 `id` = 当前最大 id + 1（空列表从 1 开始）；删除后 id 不回收。
- 将「读写 JSON / 增删改 / 备份 / 统计」抽成纯函数（`load_tasks`、`add_task`、`toggle_task`、`edit_task`、`delete_task`、`backup_tasks`、`compute_stats`），**不依赖 socket**，便于单测。

### 4.4 统计计算逻辑（`compute_stats`）
输入：全量任务列表 + 今天日期 `today`（服务端算）。输出：
```json
{
  "today_done": 3, "today_total": 5,
  "week_done": 12,
  "streak": 4,
  "today_rate": 0.6,
  "trend": [ {"date":"2026-08-24","done":1}, ... {"date":"2026-08-30","done":3} ]
}
```
- `today_*`：按 `date == today` 且 `done` 统计完成数 / 总数；完成率 = done/total（无任务记 0）。
- `week_done`：以本周一（或周日，按中文习惯取周一）为起点到今天的 `done=True` 且 `completed_at` 在该区间的数量。
- `streak`（连续打卡天数）：从今天往前数，连续每天都有「至少 1 个 `done=True` 且 `date==当天`」的最长天数；今天若无完成则 streak 从昨天起算保持逻辑一致（实现时明确：今天未完成则 streak=0 或从昨天计，文档定「从今天起，今日无完成则 streak=0」）。
- `trend`：最近 7 天（含今天），每天 `done=True` 的数量数组，供前端 SVG 画柱状图。

### 4.5 关键实现点
- 路由：用 `BaseHTTPRequestHandler`，在 `do_GET/do_POST/do_PUT/do_DELETE` 中按 `path` 分发；「业务逻辑」与「HTTP 处理」分离，使逻辑可测。
- 静态资源**防越权**：解析 `/static/...` 做路径归一化，禁止 `..` 跳出 static 目录。
- 请求体读取：`Content-Length` + `rfile.read`；JSON 用 `json.loads`。
- 响应：统一 `application/json`（接口）或 `text/html`（页面），均设 `Content-Type` 与 UTF-8。
- **端口自适应**：默认 `8000`，被占用顺延到下一个空闲端口；实际端口记 `app.log` 并用于自开浏览器。
- **隐藏运行与启停**：`start.vbs` 调 `pythonw` 启动 `app.py` → 写 PID、`webbrowser.open` 打开页面、`serve_forever()`；`stop.bat` 读 `server.pid` 执行 `taskkill /PID /F` 并删文件。`start.bat` 带「已在运行则不再启动」防护。

## 5. 前端技术方案
### 5.1 界面（米色 UI，延续 v0.1）
- 顶部：标题「每日任务清单」+ 当天日期（前端本地时间渲染）。
- **日期导航条**：`‹ 前一天` ｜ `今天` ｜ `后一天 ›`，中间显示当前查看的日期；查看非今天时高亮提示「历史日期」。
- 列表区：仅 `viewed == today` 时显示新增输入框；历史日期显示「历史日期，仅可查看」占位。
- 底部/侧：`📊 统计` 按钮，展开统计面板（指标卡 + 趋势图）。

### 5.2 与后端交互
- 进入/切日期：`GET /api/tasks?date=<viewed>` → 渲染列表 + 顶部统计「共 N · 已完成 M」（该日口径）。
- 新增：读输入框 → `POST /api/tasks`（仅 content）→ 重新拉今天列表。
- 完成/取消：点方框 → `POST /api/tasks/{id}/toggle` → 重渲染。
- **就地编辑**：**双击**任务文字 → 变 `<input>`；回车或失焦 `PUT /api/tasks/{id}`（body `{content}`）→ 重渲染；Esc 取消还原。编辑不改变 `date`。
  - **中文输入法兼容**（v1.1 实测修复）：`keydown` 中先判断 `ev.isComposing || ev.keyCode === 229`，组字/选词期间直接 `return`，把回车交给输入法确认候选词；候选词上屏后（`compositionend` 之后）再按回车才走保存分支。否则会出现「回车被输入法吃掉 → 读到空内容 → 静默放弃修改」的假死现象。
  - **空白内容不静默丢弃**：回车触发且为空 → 提示「任务内容不能为空」并 `input.focus()` 留在编辑态；失焦触发且为空 → 提示「内容为空，已还原原文」并还原（失焦时强行聚焦会造成抢焦点死循环，故不留在编辑态）。
- 删除：点删除 → `DELETE /api/tasks/{id}` → 重渲染。
- 统计：点 `📊 统计` → `GET /api/stats` → 渲染指标卡 + 近 7 天 **纯 SVG 柱状图**（今天柱子深色高亮，零依赖）。
- 渲染时对 `content` 做 HTML 转义防 XSS；空清单/空历史显示友好提示；接口失败弹小提示而非静默失败。

### 5.3 导出 / 导入（与 v0.1 一致）
- 导出：`<a href="/api/export" download="tasks.json">` 触发下载。
- 导入：`<input type="file">` → `FileReader` 读文本 → `POST /api/import` 发请求体；后端先自动备份再覆盖，成功后刷新列表。

## 6. 数据模型（tasks.json 示例，v1.1）
```json
[
  {"id": 1, "content": "复习 STM32 定时器", "done": false,
   "created_at": "2026-08-30T15:00:00", "date": "2026-08-30", "completed_at": null},
  {"id": 2, "content": "写操作系统实验报告", "done": true,
   "created_at": "2026-08-30T16:20:00", "date": "2026-08-30", "completed_at": "2026-08-30T17:05:00"},
  {"id": 3, "content": "健身 30 分钟", "done": false,
   "created_at": "2026-08-29T18:00:00", "date": "2026-08-29", "completed_at": null}
]
```

## 7. 测试方案（遵循 AGENTS.md）
- **存储层单测**（不启服务）：临时 `tasks.json`，覆盖 `add`（date=今天）/ `toggle`（写/清 `completed_at`）/ `edit` / `delete` / 旧数据归一化 / `backup` 生成 / `compute_stats`（streak、周完成、趋势、完成率）/ `export→import` 往返一致。
- **接口单测**：把路由逻辑包成可调用函数，用 `unittest.mock` 模拟请求/响应；或起 `ThreadingHTTPServer` + `urllib` 实跑（`?date` 过滤、PUT 编辑、`/api/stats`）。
- **冒烟测试**：启动 `app.py`，依次 `add → list(今天) → toggle → edit → stats → export → import → list(历史日期)`，断言状态码与数据正确，最后关服务（用系统空闲端口避免冲突）。
- 交付前 `python -m unittest` 全绿方可提交；v0.1 的 24 个旧测试保持通过。

## 8. 运行与部署（本机 · Windows）
- 日常使用：双击桌面「每日任务清单」图标（`start.vbs`）→ 后台隐藏启动 → 浏览器自动弹出清单。
- 关闭：双击 `stop.bat`（或任务管理器结束 `pythonw`）；数据已落 `tasks.json`，不丢。
- 开发调试：`python app.py`（可见窗口 + 日志），浏览器开 `http://127.0.0.1:<端口>`。
- 隐藏模式日志落 `app.log`；打不开先看该文件。首次使用需双击 `install_desktop_icon.vbs` 生成桌面图标（一次性）。

## 9. 安全与边界
- 仅监听 `127.0.0.1`，不对外网开放；单用户本地使用，**不做登录鉴权**。
- 静态资源限定 `static/` 内，禁止路径穿越（`../`）读本机其他文件。
- 输入校验：任务内容非空、长度上限（500 字）；PUT 编辑同样校验；前端转义防 XSS。
- `import` 仅接受合法任务数组（校验字段类型），忽略多余字段，防脏数据；且**导入前必做自动备份**。
- 自动备份落在 `backup/`，不纳入 git（见 `.gitignore`），避免把个人清单/运行态提交进仓库。

## 10. 风险与待确认
- `http.server` 轻量，单用户足够；`ThreadingHTTPServer` 改善并发。
- 隐藏运行（`pythonw`）下出错不弹窗，依赖 `app.log` 排查（已固化日志）。
- **streak 边界**：今天若尚无完成项，streak 计 0（与「从今天起算」一致），实现时固定此口径，避免歧义。
- **自动备份膨胀**：保留最近 30 份，超出删最旧；如需更长留存可后续调整。
- 数据文件、`server.pid`、`app.log`、`backup/`、导入备份均加入 `.gitignore`。
- 跨天顺延、标签、提醒、同步等按计划留到后续版本，不在本次实现；`date`/`completed_at` 字段已就位便于平滑升级。
