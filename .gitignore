# 运行态 / 个人数据（避免提交进仓库）
tasks.json
server.pid
app.log
app.url
start.log
start_error.txt
tasks.backup.*.json
backup/

# Python
__pycache__/
*.pyc

# Node / Capacitor（手机端）
node_modules/
npm_install.log
cap_add.log
npm-debug.log*

# Android 构建产物与本地配置
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/captures/
*.keystore
*.jks

# 打包产物（运行 打包.bat 重新生成，不入库）
dist/
gradle_build.log
assets_gen.log

# 代理/工具内部目录（非项目代码）
.workbuddy/
