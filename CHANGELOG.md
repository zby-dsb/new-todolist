# 更新日志 (Changelog)

本文件记录项目各版本的变更内容。

---

## [v1.2.0 手机端首发] — 2026-09-01

新增**手机端（Android / 鸿蒙 APK）**能力：用 Capacitor 8 把同一套网页前端装进安卓壳，
数据在手机端走 App 内本地存储（不联网），与电脑端 Python 后端并存。可产出 debug APK 侧载安装。

### 新增
- Capacitor 8 工程（`android/`），JDK 21 编译，4 个官方插件：App / Filesystem / Share / SplashScreen。
- `static/store.js`：存储 + 统计 + 快照撤销的纯函数层，浏览器与 Node 测试共用。
- 前端按平台分支：`window.Capacitor.isNativePlatform()` 为真走本地存储，否则走 `/api/...` 后端。
- 手机端交互：单击任务文字编辑（带保存/取消按钮）、安全区适配、跨天自动刷新、返回键连按退出、锁竖屏。
- 导出/导入/撤销入口收进「!」帮助弹层。
- 应用图标与启动画面（米色 `#C0A080` 同色系，取自源图主色）。
- 一键脚本：`安装到手机.bat`（adb 安装启动）、`打包.bat`（cap sync + 构建）。

### 踩坑记录（均已解决）
- **中文用户名路径**：AGP 拒绝非 ASCII 路径，加 `android.overridePathCheck=true`（暂不迁移目录）。
- **Java 版本**：Capacitor 8 插件要求 `languageVersion=21`，原 JDK 17 不满足，已装 JDK 21.0.12.1（D:\Android\JDK21）。
- **Gradle 下载超时**：wrapper 默认源 10 秒 socket 超时，改腾讯云镜像预下载 + `file:///` 本地分发。
- **`@capacitor/assets` 装不上**：依赖 sharp 需从 GitHub 下载二进制被代理掐断，改用 Pillow 脚本 `scripts/gen_icons.py` 生成图标。
- **ES module 白屏**：后端补全 `/store.js` 路由并修正 `.js` 的 MIME 为 `text/javascript`。

### 修复
- 桌面端双击编辑失效：渲染用 `task-text` 类，但双击监听找 `content` 类，两边对不上导致双击无反应（已修正）。

---

## [v1.1] — 2026-08-31

在 v1.0 基础上把「每日」语义真正落地，并补充回顾能力与数据安全保障。继续坚持**零第三方依赖**。

### 新增功能

#### 1. 每日视图
- 每条任务新增 `date` 字段（归属日期）。
- 首页默认显示「今天」的任务；顶部提供日期导航（上一天 / 下一天 / 今天），可翻看任意历史日期。
- 新增任务**只能加到今天**；历史日期可勾选完成、编辑内容、删除，但**不能往回新增**。
- 过期未完成任务**留在原日期、不顺延**，靠日期切换查看历史。

#### 2. 统计与打卡回顾
- 指标：今日完成数 / 今日总数、本周完成数、**连续打卡天数**、今日完成率。
- 近 7 天完成数趋势图（纯前端 SVG 绘制，零依赖）。
- 通过「统计」面板查看。

#### 3. 编辑任务内容（就地编辑）
- **双击**任务文字就地编辑（单击不触发，避免误触、方便选中复制文字）。
- 回车保存 / Esc 取消 / 点击别处（失焦）也保存；保存即更新，任务所属**日期不变**。

#### 4. 每次改动自动备份
- 每次数据改动后自动复制一份到项目内 `backup/` 目录，按时间戳命名。
- 保留最近 **30** 份，超出自动删除最旧的。
- 导入数据前会额外备份一次，防误删 / 损坏，可手动回滚。

### 数据模型变更（`tasks.json`）
- v1.0 字段：`id` / `content` / `done` / `created_at`
- v1.1 新增：`date`（归属日期，默认创建当天）、`completed_at`（完成时记录，取消则清空）
- **向后兼容**：旧数据缺字段时自动归一化（缺 `date` 用 `created_at` 的日期；`done` 且无 `completed_at` 时用 `created_at` 近似），**升级不丢数据**。

### 接口变更
| 方法 | 路径 | 说明 | 状态 |
|------|------|------|------|
| GET | `/api/tasks?date=YYYY-MM-DD` | 列出某天任务（缺省今天），返回 today / viewed | 变更（新增 date 参数） |
| PUT | `/api/tasks/{id}` | 编辑任务内容（日期不变） | **新增** |
| GET | `/api/stats` | 统计与打卡回顾 | **新增** |
| POST | `/api/tasks` | 新增任务（强制归属今天） | 变更 |
| POST | `/api/tasks/{id}/toggle` | 切换完成状态（写/清 completed_at） | 变更 |
| DELETE | `/api/tasks/{id}` | 删除任务 | 沿用 |
| GET | `/api/export` | 导出 json 下载 | 沿用 |
| POST | `/api/import` | 先自动备份再覆盖导入 | 沿用 |

### 修复与体验改进
- **就地编辑改为双击触发**：单击不再误触编辑，方便选中复制文字。
- **兼容中文输入法**：组字 / 选词状态下按回车由输入法确认候选词（**不保存**），候选词上屏后再按一次回车才保存。
- **空内容不再静默丢弃**：内容清空后保存会提示「任务内容不能为空」并**留在编辑状态**，不丢已输入内容。
- **接口错误提示更可读**：服务器返回 HTML 错误页时给出可读提示，而非直接抛 JSON 解析报错。

### 文档与测试
- 新增 `docs/DESIGN_v1.1.md`（v1.1 产品设计）、`docs/技术方案_v1.1.md`（技术方案）。
- 新增 `demo/archive/index_v1.1.html` 纯前端模拟原型（已归档，仅作界面参考，不参与运行）。
- `README.md` 同步更新至 v1.1（含接口概览）。
- 测试用例扩充至 **44 个**并全部通过（覆盖每日视图、统计、就地编辑、自动备份裁剪、旧数据归一化等）。

### 本次明确不做
标签 / 分类 / 优先级、提醒与通知、深色模式、拖拽排序、手机端 / 多端同步。留待后续版本。

---

## [v1.0.0] — 2026-08-30

项目首个可用版本。

- 任务的增删改查（CRUD）
- 本机单机运行，监听 `127.0.0.1:8000`，无需登录鉴权
- 数据以 UTF-8 原子写入本地 `tasks.json`，安全持久化
- 导出 / 导入功能
- 纯 Python 3 标准库后端 + 原生 HTML/JS 前端，零第三方依赖
