# 快速上手（3 分钟）

一份给第一次用的人的最短路径。想看完整说明，回 [README](README.md)。

## 功能亮点
- 📅 **每日视图**：任务归到具体日期，首页默认「今天」，顶部可翻看任意历史日期
- ✅ **统计与打卡回顾**：今日完成率、连续打卡天数、近 7 天趋势图（纯 SVG，零依赖）
- ✏️ **就地编辑**：双击任务文字改内容，回车保存 / Esc 取消
- 💾 **自动备份**：每次改动备份到 `backup/`（保留 30 份），导入前再额外备份
- 📱 **手机端**：同一套前端装进安卓壳（Capacitor 8），数据存手机本地、不联网
- 🚫 **零第三方依赖**：桌面端仅用 Python 标准库，装好 Python 就能跑

---

## 电脑端（桌面 App）

1. 确认装了 **Python 3**（无需任何其他库）。
2. 双击 `start.bat`（或 `start.vbs`）启动；首次可双击 `install_desktop_icon.vbs` 生成桌面图标。
3. 浏览器自动打开 <http://127.0.0.1:8000/>，开始加任务。
4. 顶部切日期看历史；点任务文字**双击**就地编辑；点「统计」看打卡回顾。

> 命令行方式：`python app.py`，再打开上面的地址。

## 手机端（安卓 / 鸿蒙 APK）

1. 手机：**设置 → 系统和更新 → 开发人员选项 → 打开「USB 调试」**。
2. 数据线连电脑，双击项目里的 **`安装到手机.bat`**（自动 adb 推送并启动）。
3. 手机弹窗：勾「一律允许 USB 调试」→ 确定；若「纯净模式」拦截 → 点「继续安装」。
4. 去桌面找 **「To Do」** 图标打开即可，数据存手机本地、不联网。

> 不想自己打包？去 GitHub Release 下载 `DailyTodo-v1.0-debug.apk`，手机浏览器打开后允许「未知来源」安装（侧载）。

## 小贴士
- 数据文件 `tasks.json` 在本地，不会上传；`backup/` 里能找回历史版本。
- 手机端与电脑端**数据不互通**（两套独立存储）。
- ⚠️ 鸿蒙手机**勿升级到 HarmonyOS NEXT / 5.x**（会砍掉安卓兼容层，APK 跑不了）。
